spring:
  application:
    name: tecnical-library-service
  server:
    port: 8082
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/library_db
    username: root
    password: root
  jpa:
    show-sql: true