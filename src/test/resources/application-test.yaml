logging:
  level:
    root: INFO
  kafka:
    producer:
      properties:
        batch:
          size: 0
        linger:
          ms: 0
file-service:
  url: http://localhost:9999

scheduler:
  outbox:
    batch-size: 10
    millis-delay: 5000